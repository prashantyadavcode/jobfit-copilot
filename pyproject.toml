[project]
name = "jobfit-copilot"
version = "1.0.0"
description = "Resume–job fit analyzer with NLP and LaTeX rewrite support"
requires-python = ">=3.11"

[tool.vercel]
entrypoint = "app.main:app"

[tool.vercel.scripts]
build = "python scripts/vercel_build.py"
